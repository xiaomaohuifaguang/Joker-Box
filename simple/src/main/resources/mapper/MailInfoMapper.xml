<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cat.simple.mapper.MailInfoMapper">

    <select id="selectPage" resultType="com.cat.common.entity.mail.MailInfo">
        SELECT id, to_mail, subject, send_time FROM cat_mail_info
        <where>
            <if test='param.search != null and param.search != ""'>
                and (
                to_mail like concat('%',#{param.search},'%')
                or subject like concat('%',#{param.search},'%')
                or content like concat('%',#{param.search},'%')
                or variable like concat('%',#{param.search},'%')
                )
            </if>
        </where>
    </select>
</mapper>